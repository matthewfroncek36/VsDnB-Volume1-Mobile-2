class SupernovaeDialogue extends Dialogue
{
    public function new()
    {
        super('supernovae');
    }

    override function buildMusic():Void
    {
        if (FlxG.random.int(0, 50) == 50)
        {
            // Change the music path to the seeeecret song...
            dialogueMusicPath = 'secret';
        }
        super.buildMusic();
    }
}